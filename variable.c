#include <stdio.h>
int main(){
	int a; // if we donot initialise the variable then the default value is assigned is 0 and a memory is also allocated to that variable
    printf("%d",a);
    return 0;
}
